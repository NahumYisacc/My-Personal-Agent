[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "progress-tracker-agent"
version = "0.1.0"
description = "A lightweight progress tracker agent"
requires-python = ">=3.10"
